# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'kinda-news-lenta' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Networking
  pod 'Alamofire', '~> 5.6'
  
  # Layout
  pod 'SnapKit', '~> 5.7.1'
  
  # Optional: For image caching (recommended)
  pod 'Kingfisher', '~> 8.1.0'
  # Pods for kinda-news-lenta

  target 'kinda-news-lentaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'kinda-news-lentaUITests' do
    # Pods for testing
  end

end
